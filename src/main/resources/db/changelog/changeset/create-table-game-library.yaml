databaseChangeLog:
  - changeSet:
      id: create-game-libraries-table
      author: Sweet Supremum
      changes:
        - createTable:
            tableName: game_library
            columns:
              - column:
                  name: game_id
                  type: varchar(200)
                  constraints:
                    primaryKey: true
                    nullable: false

